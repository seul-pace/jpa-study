server:
  port: 8081

spring:
# 지워도 메모리로 동작함 (springboot 최고)
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 mode
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # 마지막에 drop 쿼리 날림
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace